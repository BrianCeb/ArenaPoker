# Copiar este archivo a .env y completar los valores reales.
# El .env real NUNCA se sube al repositorio (ver .gitignore).

DATABASE_URL="postgresql://mesasvivas:mesasvivas_dev@localhost:5432/mesas_vivas?schema=public"

PORT=3000

# Se completan en fases posteriores (auth, email, etc.)
# JWT_SECRET=
# RESEND_API_KEY=
